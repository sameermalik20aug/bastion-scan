# Base URL of the Bastion backend API (no trailing slash).
# The frontend appends /api/v1/scan to this value.
# Leave empty to use a same-origin / proxied backend.
VITE_API_BASE_URL=http://localhost:8000
